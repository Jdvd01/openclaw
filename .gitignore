# Tokens de autenticación (Zapier MCP - OAuth)
config/mcporter.json

# Metadata de entorno (no útil en repo)
openclaw-workspace-state.json

# Archivo de notas internas (diseñado para datos sensibles como SSH, API keys, etc.)
TOOLS.md

# Metadata interna de ClawHub (marketplace de skills)
.clawhub/